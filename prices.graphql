directive @tag(name: String!) repeatable on
  | FIELD_DEFINITION
  | INTERFACE
  | OBJECT
  | UNION

type Price @key(fields: "id") {
    id: ID! 
    usdPrice: Float
    priceDecorator: String 
}

extend type Query {
    prices: [Price] 
    price(id: ID!): Price!
}